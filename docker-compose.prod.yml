# Optional production override: static nginx web instead of Vite.
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up --build
services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile.prod
      args:
        VITE_API_URL: /api
        VITE_STATIC_URL: ""
    ports:
      - "${WEB_PORT:-8080}:80"
    volumes: []
    environment: {}
